# Dependencies & build artifacts
node_modules/
dist/

# Environment (commit .env.example instead)
.env
.env.local

# Binary deliverables & assets not needed in source control
*.docx
*.pdf
mahesh.png

# Scratch/working media dropped at the repo root (real app user-media lives on
# the backend in _media/, not here). Anchored so tracked assets under
# public/formats/ are unaffected.
/*.mp4
/*.mov
/*.webm
/public/fixit/

# Editor/OS noise
.DS_Store
*.log
