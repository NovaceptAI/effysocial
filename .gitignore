# Dependencies & build artifacts
node_modules/
dist/
__pycache__/
*.pyc

# Environment (commit .env.example instead)
.env
.env.local

# Binary deliverables & assets not needed in source control
*.docx
*.pdf
mahesh.png

# Scratch/working media dropped at the repo root (real app user-media lives on
# the backend in _media/, not here). Anchored so tracked assets under
# public/formats/ are unaffected.
/*.mp4
/*.mov
/*.webm
/*.mp3
/public/fixit/
/scripts/_onam/
/public/onam/
/public/landing/
/IMG-*.jpg
/roff_3d_render*.png

# One-off campaign tooling. Kept out of the app history; the scripts and their
# renders are archived alongside the deliverables under public/onam/.
/scripts/

# Editor/OS noise
.DS_Store
*.log
.engine-staging/
